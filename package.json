{
  "name": "bram-the-almost-boy-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Playable Phaser starter prototype for BRAM: The Almost Boy, a cozy math adventure game.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
